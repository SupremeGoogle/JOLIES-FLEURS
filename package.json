{
  "name": "jolies-fleurs-deploy",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "npm ci --prefix web && npm run build --prefix web && mkdir -p web/.next/standalone/.next && cp -R web/.next/static web/.next/standalone/.next/static && cp -R web/public web/.next/standalone/public",
    "start": "cd web/.next/standalone && node server.js",
    "lint": "npm run lint --prefix web"
  },
  "engines": {
    "node": ">=22 <23",
    "npm": ">=10"
  }
}
